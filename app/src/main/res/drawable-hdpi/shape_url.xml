<?xml version="1.0" encoding="UTF-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充的颜色 -->
    <solid android:color="@color/listItem_white"/>
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 四个角半径值
         android:topLeftRadius左上角半径值
         android:topRightRadius右上角半径值
         android:bottomLeftRadius左下角半径值
         android:bottomRightRadius右下角半径值 -->
    <corners
        android:radius="6dp"/>
    <!--android:width 描边宽度
        android:color 描边颜色
        android:dashwidth描边样式为虚线时的宽度，值为0时为实线，值大于0时为虚线
        android:dashGap 描边为虚线时，虚线之间的间隔 -->
    <stroke android:color="@color/listLine" android:width="0.5dp" />
</shape>